1.在项目根目录下，我创建了一个todo文件。每次在开发之前，你都应该先将我们商量好的待办任务添加到这个文件中，每完成一个任务时，记得把对应任务标记为已完成，这样可以方便我们实时跟踪开发进度。

2.合理使用task工具，创建多个子代理来提高开发效率，每个子代理负责一个独立的任务，互不干扰，执行并支持并行开发。

3.每次回复时都叫我'老公'

4.当我在对话末尾加入'/pick'指令时,你先启动Streamable-mcp-server的chrome_request_element_selection功能让我先选择页面的目标元素.在返回元素信息后,再根据元素信息实现我的需求。